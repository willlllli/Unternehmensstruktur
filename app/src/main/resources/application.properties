spring.application.name=unternehmensstruktur

# PostgreSQL Datenbank
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/unternehmenshierarchie}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# HikariCP: search_path fuer JdbcTemplate setzen (wird bei jedem neuen Connection-Pool-Eintrag ausgefuehrt)
spring.datasource.hikari.connection-init-sql=SET search_path TO unternehmensstruktur, public

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.default_schema=unternehmensstruktur

server.port=8080
